import React from 'react';

interface Props {}

const Typography = (props: Props) => {
  return (
    <div
      style={{
        backgroundAttachment: '',
      }}>
      {/* {eStylesGroup.TEXT} */}
    </div>
  );
};

export default Typography;

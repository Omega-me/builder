export enum eDeviceTypes {
  DESKTOP = 'Desktop',
  MOBILE = 'Mobile',
  TABLET = 'Tablet',
}

export enum eEditorBtns {
  TEXT = 'text',
  CONTAINER = 'container',
  SECTION = 'section',
  LINK = 'link',
  TWO_COL = '2Col',
  VIDEO = 'video',
  BODY = '__body',
  IMAGE = 'image',
  NULL = 'null',
  THREE_COL = '3Col',
}

export enum eComponentsGroup {
  LAYOUT = 'layout',
  ELEMENTS = 'elements',
}
